{
  "name": "bartools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test packages/ui/src/greet.test.ts && bun --filter @bartools/dashboard test",
    "dev:backend": "bun --filter @bartools/backend dev",
    "dev:dashboard": "bun --filter @bartools/dashboard dev",
    "dev:mobile": "bun --filter @bartools/mobile start"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
